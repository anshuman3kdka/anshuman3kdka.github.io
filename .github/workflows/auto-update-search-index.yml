name: Auto-update random read index

on:
  push:
    branches:
      - main
    paths:
      - '**/*.md'
      - '**/*.html'
      - 'scripts/generate-random-read-index.mjs'
      - 'package.json'
      - '.github/workflows/auto-update-search-index.yml'
  workflow_dispatch:

jobs:
  update-index:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Regenerate random read index
        run: npm run generate:random-read-index

      - name: Commit updated random read index
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add random-read.json
          if git diff --cached --quiet; then
            echo "No random read index changes to commit."
            exit 0
          fi
          git commit -m "chore: auto-update random read index"
          git push
