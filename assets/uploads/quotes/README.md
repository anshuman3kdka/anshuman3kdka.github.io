# Quote PDF upload folder

Put your hero quote PDF in this folder with this exact name:

- `hero-quotes.pdf`

Final path:

- `assets/uploads/quotes/hero-quotes.pdf`

After uploading/replacing it, run:

```bash
npm run build
```

That regenerates `_data/hero_quotes.json`, and the homepage quote rotator updates automatically.
